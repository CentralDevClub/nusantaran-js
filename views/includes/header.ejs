<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lora">
    <link rel="stylesheet" href="/fonts/font-awesome.min.css">
    <link rel="stylesheet" href="/fonts/ionicons.min.css">
    <link rel="stylesheet" href="/css/styles.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <header class="header">
        <nav class="navbar navbar-light navbar-expand-md navigation-clean-button">
            <div class="container"><a class="navbar-brand" href="/">Nusantaran</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navcol-1">
                    <ul class="nav navbar-nav mr-auto">
                        <li class="nav-item mr-2"><a href="/products" class="<%= path.includes('/products') ? 'active' : '' %>">Shop</a></li>
                        <% if (isAuthenticated){ %>
                            <li class="nav-item mr-2"><a href="/cart" class="<%= path === '/cart' ? 'active' : '' %>">Cart</a></li>
                            <li class="nav-item mr-2"><a href="/checkout" class="<%= path === '/checkout' ? 'active' : '' %>">Checkout</a></li>
                            <li class="nav-item mr-2"><a href="/admin/product" class="<%= path === '/product' ? 'active' : '' %>">My Products</a></li>
                        <% } %>
                    </ul>
                    <% if (!isAuthenticated) { %>
                        <span class="navbar-text actions">
                            <a class="login" href="/login">Log In</a>
                            <a class="btn btn-primary action-button" role="button" href="/register">Sign Up</a>
                        </span>
                    <% } else { %>
                        <div class="navbar-text actions">
                            <% if (isAuthenticated){ %>
                                <a class="mr-2" href="/profile">My Account</a>
                            <% } %>
                            <form style="display: inline;" action="/logout" method="POST">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button type="submit" class="btn btn-info">Logout</button>
                            </form>
                        </span>
                    <% } %>
                </div>
            </div>
        </nav>
    </header>